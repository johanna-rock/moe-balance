{
  // Search type
  // Places most-used experts in a hot tier (few rows), rest elsewhere.
  "search": "hot-tier", // place top-N experts in a hot tier

  // Number of randomized restarts
  "restarts": 1 // repeat search with different seeds
}
