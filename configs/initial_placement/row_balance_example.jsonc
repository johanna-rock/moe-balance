// Initial placement config
{
  "placement_strategy": "row-balance", // Options: row-balance, row-aware, hot-tier
  "shared_expert_round_robin_in_rows": true // place shared replicas round-robin across rows+cols
}
