// Initial placement config
{
  "placement_strategy": "hot-tier", // Options: balance, balance_load_coactivation, hot-tier
  "placement_axis": "row", // balance experts across row or col
  "hot_tier_axis": "col", // For hot-tier: distribute hot experts across row or col
  "replica_spread_by_distance": true // place shared replicas round-robin across rows+cols
}
