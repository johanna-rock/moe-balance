{
  // Search type
  // Evaluates random placements and keeps the best.
  "search": "random", // random placements, keep best

  // Random search iterations
  "iters": 200, // number of random placements to evaluate

  // Number of randomized restarts
  "restarts": 2 // repeat search with different seeds
}
